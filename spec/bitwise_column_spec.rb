require 'spec_helper'

describe BitwiseColumn do
  it 'has a version number' do
    expect(BitwiseColumn::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
